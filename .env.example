# Gmail API Credentials
GMAIL_CREDENTIALS_FILE=config/gmail_credentials.json
GMAIL_TOKEN_FILE=config/gmail_token.json

# LLM Configuration
# MLX Server Configuration (via Tailscale)
# The application connects to an MLX server running on your macOS laptop.
# See docs/mlx_server_setup.md for server setup instructions.
# See docs/development_environment.md for client configuration.

# MLX Server URL (REQUIRED)
# - Development (Codespaces): Use your laptop's Tailscale IP
#   Get it with: tailscale ip -4 (on your macOS laptop)
#   Example: MLX_SERVER_URL=http://100.64.0.123:8080
# - Production (on macOS laptop): Use localhost
#   Example: MLX_SERVER_URL=http://localhost:8080
MLX_SERVER_URL=http://100.64.0.123:8080

# LLM Model (must match model loaded on MLX server)
# Recommended models:
# - mlx-community/Llama-3.2-1B-Instruct-4bit (fast, ~0.5GB)
# - mlx-community/Llama-3.2-3B-Instruct-4bit (recommended, ~1.5GB)
# - mlx-community/Mistral-7B-Instruct-v0.3-4bit (best quality, ~4GB)
LLM_MODEL=mlx-community/Llama-3.2-3B-Instruct-4bit

# ALTERNATIVE: Use OpenAI for testing (requires API key)
# Uncomment these and comment out MLX_SERVER_URL above:
# MLX_SERVER_URL=https://api.openai.com
# LLM_MODEL=gpt-4o-mini
# OPENAI_API_KEY=your-api-key-here

# Label Configuration
LABEL_CONFIG_FILE=config/labels.json

# Logging
LOG_LEVEL=INFO
LOG_FILE=data/logs/email_triage.log
